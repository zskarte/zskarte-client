import { RegexValidatorDirective } from './regex-validator.directive';

describe('RegexValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RegexValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
